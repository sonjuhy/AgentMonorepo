# Orchestra Agent Dependencies

# FastAPI & ASGI
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Pydantic v2
pydantic>=2.7.0

# Redis (async)
redis[hiredis]>=5.0.0

# Google Gemini (Primary NLU)
google-genai>=1.0.0

# Anthropic Claude (Fallback NLU)
anthropic>=0.40.0

# PostgreSQL (optional — for persistent session history)
asyncpg>=0.30.0

# HTTP client for dispatching tasks to sub-agents
httpx>=0.27.0

# Timezone support (Python 3.9+ zoneinfo backport for older envs)
tzdata>=2024.1
