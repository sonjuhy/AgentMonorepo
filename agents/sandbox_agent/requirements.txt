# Sandbox Agent Dependencies

# HTTP (OrchestraManager 결과 보고)
httpx>=0.27.0

# Redis (OrchestraManager 큐 수신)
redis[hiredis]>=5.0.0

# FastAPI & ASGI (server 모드)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Pydantic v2 (요청 바디 검증)
pydantic>=2.7.0

# 환경변수 로딩
python-dotenv>=1.0.0
